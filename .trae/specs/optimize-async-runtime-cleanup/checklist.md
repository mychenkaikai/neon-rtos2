- [x] `Sleep` 不再依赖占位式 TODO 或仅克隆 `waker` 的实现
- [x] 异步休眠任务在截止时间到达后可以被重新轮询并完成
- [x] `Executor::run` 与 `Executor::poll_once` 共享一致的核心轮询逻辑
- [x] 执行器在没有可运行异步任务时仍保持当前的空闲阻塞语义
- [x] 本轮变更包含针对异步休眠或执行器唤醒路径的验证
- [x] 运行时说明已更新，并明确本轮优化范围
